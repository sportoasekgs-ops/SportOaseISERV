{% extends '@SportOase/base.html.twig' %}

{% block title %}Feste Angebote verwalten{% endblock %}

{% block header %}
    <div class="flex items-center justify-between">
        <h2 class="text-2xl font-bold text-gray-900">
            <svg class="w-8 h-8 inline mr-2 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z"></path>
            </svg>
            Feste Angebote verwalten
        </h2>
        <a href="{{ path('sportoase_admin_dashboard') }}" 
           class="inline-flex items-center px-4 py-2 border border-gray-300 rounded-lg text-sm font-medium text-gray-700 bg-white hover:bg-gray-50 transition">
            <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"></path>
            </svg>
            Zurück zur Admin-Übersicht
        </a>
    </div>
{% endblock %}

{% block content %}
    <div class="bg-white rounded-xl shadow-md overflow-hidden mb-8">
        <div class="px-6 py-5 border-b border-gray-200">
            <h3 class="text-xl font-semibold text-gray-900 flex items-center gap-2">
                <svg class="w-6 h-6 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"></path>
                </svg>
                Angebote umbenennen
            </h3>
            <p class="text-sm text-gray-600 mt-2">
                Hier können Sie die Namen der festen Angebote anpassen. Die ursprünglichen Namen bleiben als Schlüssel erhalten.
            </p>
        </div>
        
        <div class="px-6 py-4">
            {% for offer_name in offer_names %}
            <div class="mb-6 p-4 border border-gray-200 rounded-lg hover:border-primary-300 transition">
                <form method="post" action="{{ path('sportoase_admin_update_offer_name') }}" class="space-y-4">
                    <div class="flex items-center justify-between">
                        <div>
                            <label class="block text-sm font-medium text-gray-700">
                                Original-Name
                            </label>
                            <p class="text-lg font-semibold text-gray-900">{{ offer_name.defaultName }}</p>
                        </div>
                        <div class="flex-1 max-w-md ml-6">
                            <label for="custom_name_{{ offer_name.id }}" class="block text-sm font-medium text-gray-700 mb-1">
                                Benutzerdefinierter Name
                            </label>
                            <input type="hidden" name="offer_key" value="{{ offer_name.offerKey }}">
                            <input type="text" 
                                   id="custom_name_{{ offer_name.id }}" 
                                   name="custom_name" 
                                   value="{{ offer_name.customName }}" 
                                   class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-primary-500 focus:border-transparent"
                                   placeholder="{{ offer_name.defaultName }}">
                        </div>
                        <div class="ml-4">
                            <button type="submit" 
                                    class="inline-flex items-center px-4 py-2 bg-primary-600 hover:bg-primary-700 text-white text-sm font-medium rounded-lg shadow-sm transition">
                                <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                                </svg>
                                Speichern
                            </button>
                        </div>
                    </div>
                </form>
            </div>
            {% endfor %}
        </div>
    </div>

    <div class="bg-white rounded-xl shadow-md overflow-hidden">
        <div class="px-6 py-5 border-b border-gray-200">
            <h3 class="text-xl font-semibold text-gray-900 flex items-center gap-2">
                <svg class="w-6 h-6 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
                </svg>
                Platzierungen der festen Angebote
            </h3>
            <p class="text-sm text-gray-600 mt-2">
                Übersicht, wann welche festen Angebote stattfinden.
            </p>
        </div>
        
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-semibold text-gray-700 uppercase tracking-wider">Wochentag</th>
                        <th class="px-6 py-3 text-left text-xs font-semibold text-gray-700 uppercase tracking-wider">Stunde</th>
                        <th class="px-6 py-3 text-left text-xs font-semibold text-gray-700 uppercase tracking-wider">Angebot</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200">
                    {% set weekdays = {1: 'Montag', 2: 'Dienstag', 3: 'Mittwoch', 4: 'Donnerstag', 5: 'Freitag'} %}
                    {% for placement in placements %}
                    <tr class="hover:bg-gray-50 transition">
                        <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                            {{ weekdays[placement.weekday] }}
                        </td>
                        <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-700">
                            {{ placement.period }}. Stunde
                        </td>
                        <td class="px-6 py-4 whitespace-nowrap">
                            <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-yellow-100 text-yellow-800">
                                ⭐ {{ placement.offerName }}
                            </span>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    <div class="mt-6 bg-blue-50 border-l-4 border-blue-500 rounded-lg p-4">
        <div class="flex">
            <div class="flex-shrink-0">
                <svg class="h-5 w-5 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                </svg>
            </div>
            <div class="ml-3">
                <h3 class="text-sm font-medium text-blue-800">Hinweis</h3>
                <div class="mt-2 text-sm text-blue-700">
                    <p>
                        Feste Angebote werden in der Wochenansicht <strong>gelb markiert</strong> angezeigt. 
                        Lehrkräfte können diese Slots trotzdem buchen, müssen aber das entsprechende Modul auswählen.
                    </p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
